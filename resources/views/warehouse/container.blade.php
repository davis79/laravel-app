<x-warehouse-shell :title="$container->container_number" description="Szczegóły kontenera i historia wykorzystania w produkcji."><x-slot:breadcrumbs><span>/ {{ $container->flavor->product->name }} / {{ $container->flavor->name }} / {{ $container->container_number }}</span></x-slot:breadcrumbs><div class="grid gap-6 lg:grid-cols-[1fr_1.4fr]"><div><section class="rounded-2xl border border-white/[0.06] bg-[#0d1024] p-6"><h2 class="font-bold text-white">Bilans kontenera</h2><div class="mt-6 grid grid-cols-2 gap-4"><div class="rounded-xl bg-white/[0.03] p-4"><p class="text-xs text-slate-500">Waga początkowa</p><b class="mt-1 block text-xl text-white">{{ $container->weight_kg }} kg</b></div><div class="rounded-xl bg-white/[0.03] p-4"><p class="text-xs text-slate-500">Zużyto</p><b class="mt-1 block text-xl text-amber-300">{{ number_format((float)$container->usages_sum_quantity_kg,0,',',' ') }} kg</b></div><div class="col-span-2 rounded-xl bg-indigo-500/10 p-4"><p class="text-xs text-indigo-300">Pozostało</p><b class="mt-1 block text-2xl text-white">{{ number_format($container->remaining_weight,0,',',' ') }} kg</b></div></div></section>@if($container->is_active)<form method="POST" action="{{ route('warehouse.usages.store',$container) }}" class="mt-6 rounded-2xl border border-white/[0.06] bg-[#0d1024] p-6">@csrf<h2 class="font-bold text-white">Zarejestruj użycie</h2><div class="mt-5 space-y-3"><input name="production_number" value="{{ old('production_number') }}" required placeholder="Nr produkcji" class="w-full rounded-xl bg-[#080b1b] px-4 py-3"><div class="grid grid-cols-2 gap-3"><input name="quantity_kg" type="number" step="0.001" required placeholder="Ilość kg" class="rounded-xl bg-[#080b1b] px-4 py-3"><input name="used_at" type="datetime-local" required value="{{ now()->format('Y-m-d\TH:i') }}" class="rounded-xl bg-[#080b1b] px-4 py-3"></div><textarea name="notes" placeholder="Uwagi" class="w-full rounded-xl bg-[#080b1b] px-4 py-3"></textarea>@if($errors->any())<p class="text-sm text-rose-400">{{ $errors->first() }}</p>@endif<button class="w-full rounded-xl bg-indigo-500 px-4 py-3 font-semibold text-white">Zapisz pobranie</button></div></form>@else<div class="mt-6 rounded-2xl border border-slate-500/10 bg-slate-500/5 p-6 text-center"><span class="rounded-full bg-slate-500/10 px-3 py-1 text-xs font-semibold text-slate-400">Nieaktywny</span><p class="mt-3 text-sm text-slate-500">Cała zawartość kontenera została zużyta. Kolejne pobrania są zablokowane.</p></div>@endif</div><section class="rounded-2xl border border-white/[0.06] bg-[#0d1024] p-6"><h2 class="font-bold text-white">Historia wykorzystania</h2><div class="mt-5 space-y-3">@forelse($container->usages->sortByDesc('used_at') as $usage)<article class="rounded-xl border border-white/[0.05] bg-white/[0.02] p-4"><div class="flex justify-between gap-3"><div><h3 class="font-semibold text-white">Produkcja {{ $usage->production_number ?: $usage->production_name }}</h3><p class="mt-1 text-xs text-slate-500">{{ $usage->used_at->format('d.m.Y H:i') }}</p></div><b class="shrink-0 text-amber-300">-{{ $usage->quantity_kg }} kg</b></div>@if($usage->notes)<p class="mt-3 text-sm text-slate-400">{{ $usage->notes }}</p>@endif<p class="mt-2 text-[10px] text-slate-600">Zapisał: {{ $usage->recorder?->name ?? 'usunięty użytkownik' }}</p></article>@empty<p class="py-16 text-center text-slate-500">Kontener nie został jeszcze użyty.</p>@endforelse</div></section></div></x-warehouse-shell>
